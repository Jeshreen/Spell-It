import { shallowMount, shallow } from '@vue/test-utils'
import UserCard from '@/components/UserCard'

/**
 * Test 1 - Mounting the User card component
 */
describe('Mount Component', () => {
    const wrapper = shallow(UserCard);
})